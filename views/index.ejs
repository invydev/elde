<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BuyPanel - Panel Pterodactyl</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- SweetAlert2 -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    :root {
      --primary: #4361ee;
      --primary-dark: #3a56e4;
      --secondary: #6c757d;
      --success: #06d6a0;
      --info: #118ab2;
      --warning: #ffd166;
      --danger: #ef476f;
      --light: #f8f9fa;
      --dark: #1e1e2d;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .card {
      border-radius: 15px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
      border: none;
      overflow: hidden;
    }

    .card-header {
      background: var(--primary);
      color: white;
      font-weight: 600;
      border-radius: 15px 15px 0 0 !important;
      padding: 1.2rem;
    }

    .btn-primary {
      background-color: var(--primary);
      border-color: var(--primary);
      padding: 10px 20px;
      font-weight: 500;
    }

    .btn-primary:hover {
      background-color: var(--primary-dark);
      border-color: var(--primary-dark);
    }

    .form-control, .form-select {
      border-radius: 8px;
      padding: 12px 15px;
      border: 1px solid #ddd;
    }

    .form-control:focus, .form-select:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.25);
    }

    .logo {
      text-align: center;
      margin-bottom: 20px;
    }

    .logo h1 {
      font-weight: 700;
      color: var(--primary);
      text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
    }

    .qr-container {
      text-align: center;
      padding: 20px;
    }

    .qr-image {
      max-width: 250px;
      margin: 0 auto;
      border: 10px solid white;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }

    .status-badge {
      display: inline-block;
      padding: 8px 15px;
      border-radius: 50px;
      font-weight: 500;
      margin-top: 15px;
    }

    .status-pending {
      background-color: #fff3cd;
      color: #856404;
    }

    .status-success {
      background-color: #d4edda;
      color: #155724;
    }

    .status-failed {
      background-color: #f8d7da;
      color: #721c24;
    }

    .panel-details {
      background-color: var(--light);
      border-radius: 10px;
      padding: 20px;
      margin-top: 20px;
    }

    .info-box {
      background: white;
      border-radius: 10px;
      padding: 15px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.05);
      margin-bottom: 15px;
    }

    .info-box h5 {
      font-size: 16px;
      color: var(--secondary);
      margin-bottom: 5px;
    }

    .info-box p {
      font-size: 18px;
      font-weight: 500;
      margin-bottom: 0;
      color: var(--dark);
    }

    .loading {
      display: none;
      text-align: center;
      padding: 20px;
    }

    .spinner {
      width: 3rem;
      height: 3rem;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="container py-4">
    <div class="logo">
      <h1>BuyPanel</h1>
      <p class="text-muted">Beli Panel Pterodactyl dengan Mudah</p>
    </div>

    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header text-center">
            <h4 class="mb-0">Form Pembelian Panel</h4>
          </div>
          <div class="card-body">
            <form id="orderForm">
              <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input type="text" class="form-control" id="username" placeholder="Contoh: myserver123" required>
              </div>
              
              <div class="mb-4">
                <label for="ram" class="form-label">Pilih RAM</label>
                <select class="form-select" id="ram" required>
                  <option value="">-- Pilih RAM --</option>
                  <% for (const [ram, price] of Object.entries(ramOptions)) { %>
                    <option value="<%= ram %>"><%= ram.toUpperCase() %> - Rp <%= price.toLocaleString() %></option>
                  <% } %>
                </select>
              </div>
              
              <div class="d-grid">
                <button type="submit" class="btn btn-primary btn-lg">Beli Sekarang</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- QR Container -->
    <div class="row justify-content-center mt-4">
      <div class="col-md-8">
        <div class="card" id="qrCard" style="display: none;">
          <div class="card-header text-center">
            <h4 class="mb-0">Pembayaran QRIS</h4>
          </div>
          <div class="card-body">
            <div class="loading" id="loading">
              <div class="spinner-border text-primary" role="status"></div>
              <p>Sedang memproses pembayaran...</p>
            </div>
            <div class="qr-container" id="qrDisplay">
              <p class="text-muted">Scan QRIS di bawah ini menggunakan aplikasi e-wallet (GoPay, OVO, Dana, dll)</p>
              <img id="qrImage" class="qr-image img-fluid" src="" alt="QR Code">
              <div class="mt-3">
                <h5>Total Pembayaran</h5>
                <h3 id="amountDisplay" class="text-dark fw-bold"></h3>
              </div>
              <div class="mt-3">
                <span id="statusBadge" class="status-badge status-pending">Menunggu Pembayaran</span>
              </div>
            </div>

            <div id="panelDetails" class="panel-details mt-4" style="display: none;">
              <h4 class="text-center mb-4">Detail Panel Anda</h4>
              <div class="row" id="detailContent">
                <!-- Detail panel akan diisi di sini -->
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('orderForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const username = document.getElementById('username').value;
      const ram = document.getElementById('ram').value;
      
      // Tampilkan card QR
      document.getElementById('qrCard').style.display = 'block';
      // Tampilkan loading
      document.getElementById('loading').style.display = 'block';
      // Sembunyikan QR sementara
      document.getElementById('qrDisplay').style.display = 'none';
      // Sembunyikan panel details jika ada
      document.getElementById('panelDetails').style.display = 'none';

      try {
        const response = await fetch('/order', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({ username, ram })
        });
        
        const data = await response.json();
        
        if (data.error) {
          Swal.fire({
            icon: 'error',
            title: 'Error!',
            text: data.error,
          });
          document.getElementById('qrCard').style.display = 'none';
          return;
        }
        
        // Sembunyikan loading
        document.getElementById('loading').style.display = 'none';
        // Tampilkan QR
        document.getElementById('qrDisplay').style.display = 'block';
        
        // Set QR image
        document.getElementById('qrImage').src = data.qrImage;
        document.getElementById('amountDisplay').textContent = `Rp ${data.amount.toLocaleString()}`;
        
        // Polling untuk cek status pembayaran
        const checkPayment = setInterval(async () => {
          const statusRes = await fetch('/check-order');
          const statusData = await statusRes.json();
          
          if (statusData.status === 'success') {
            clearInterval(checkPayment);
            document.getElementById('statusBadge').className = 'status-badge status-success';
            document.getElementById('statusBadge').textContent = 'Pembayaran Berhasil!';
            
            // Tampilkan detail panel
            const panel = statusData.panelData;
            const detailContent = `
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>ID Server</h5>
                  <p>${panel.serverId}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Nama</h5>
                  <p>${panel.name}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Username</h5>
                  <p>${panel.username}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Password</h5>
                  <p>${panel.password}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Login URL</h5>
                  <p><a href="${panel.loginUrl}" target="_blank">${panel.loginUrl}</a></p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>RAM</h5>
                  <p>${panel.ram}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>CPU</h5>
                  <p>${panel.cpu}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Disk</h5>
                  <p>${panel.disk}</p>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <div class="info-box">
                  <h5>Expired</h5>
                  <p>${panel.expiry}</p>
                </div>
              </div>
              <div class="col-12 mt-4">
                <div class="alert alert-warning">
                  <strong>Perhatian!</strong> Simpan data ini dengan baik. Garansi hanya berlaku 10 hari. Data yang hilang karena kelalaian pengguna tidak dapat diklaim.
                </div>
              </div>
            `;
            
            document.getElementById('detailContent').innerHTML = detailContent;
            document.getElementById('panelDetails').style.display = 'block';
            
            Swal.fire({
              icon: 'success',
              title: 'Sukses!',
              text: 'Panel berhasil dibuat!',
            });
            
          } else if (statusData.status === 'failed') {
            clearInterval(checkPayment);
            document.getElementById('statusBadge').className = 'status-badge status-failed';
            document.getElementById('statusBadge').textContent = 'Pembayaran Gagal';
            Swal.fire({
              icon: 'error',
              title: 'Gagal!',
              text: 'Pembayaran gagal atau dibatalkan',
            });
          }
        }, 5000); // Cek setiap 5 detik
        
      } catch (error) {
        console.log('Error:', error);
        Swal.fire({
          icon: 'error',
          title: 'Error!',
          text: 'Terjadi kesalahan saat memproses',
        });
        document.getElementById('loading').style.display = 'none';
      }
    });
  </script>
</body>
</html>